- name: Deploy Virtual Zoo to Kubernetes
  hosts: localhost
  connection: local
  tasks:
    - name: Apply Kubernetes Deployment and Service
      # This is the corrected module name
      kubernetes.core.k8s:
        state: present
        src: "{{ item }}"
      loop:
        - "k8s/deployment.yaml"
        - "k8s/service.yaml"
